# Python
__pycache__/
*.py[cod]
.venv/

# ビルド成果物（旧構成。現在は htmls/ に集約し、htmls/ は追跡する）
_site/

# ローカル設定（.claude/ の規約・スキルは追跡する）
.claude/settings.local.json

# 第三者クローン（README の手順で取得・パッチ。patches/ に差分を保存）
src/

# 大きいデータ・合成 CSV（再取得/再生成可能）
data/*.csv
outputs/*.csv
